---
- name: "[ YAY ]"
  hosts: all
  tasks:
    - name: Install yay
      become: true
      community.general.pacman:
        name: "{{ item }}"
        state: present
      loop:
        - yay
        - pkgconf
        - base-devel
        - make
